# 数据库表（必建）
sys_user         # 用户
sys_role         # 角色
sys_menu         # 菜单 + 权限按钮
sys_user_role    # 用户-角色 关联
sys_role_menu    # 角色-菜单 关联
sys_dict         # 字典
sys_log          # 操作日志

# 权限控制逻辑
登录 → 生成 JWT
请求 → JWT 拦截验证
Spring Security 鉴权
接口使用 @PreAuthorize("@pms.hasPerm('sys:user:list')")