spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/symmall?useUnicode=true&characterEncoding=utf8&autoReconnect=true&allowMultiQueries=true&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: passw0rD


mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.sym.**.domain # 实体类包
  configuration:
    map-underscore-to-camel-case: true  # 自动下划线转驼峰
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # SQL打印
  global-config:
    db-config:
      id-type: assign_id  # 雪花ID
      logic-delete-field: deleted  # 全局逻辑删除字段
      logic-delete-value: 1
      logic-not-delete-value: 0